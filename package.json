{
  "name": "pokelist",
  "private": true,
  "version": "1.0.0",
  "description": "PokéList - Pokédex Kanto/Johto/Hoenn avec mode Quiz (PWA statique, sans build).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
